#include<stdio.h>
#include<stdlib.h>


enum name{
		kamal,
		montu,
		vineet,
		logus,
		arpit,
		shakshi,
		vimal,
		golu,
		karan,
		rajat,
		puskar,
		anita=10001,
		vikas,
		pulkit,
		sagar,
		jonti,
		sandeep,
		gurpreet,

	 }c;


int main(){

while(1){
	c = vikas;
	printf("Random Pick: %d \n",c);
	sleep(1);
}
return 0;
}
